
define([
    "jquery",
    "underscore",
    "backbone",    
    "models/console-cell-model"
    ], function( $, _, BackBone, CellModel){

	var Console = Backbone.View.extend({

	    initialize : function (){

	    }

	});

	return Console;

    });
