define([
    "jquery",
    "underscore",
    "backbone",    
    "views/console-cell"
    ], function( $, _, BackBone, Cell){

	var Console = Backbone.View.extend({

	    initialize : function (){

	    }

	});

	return Console;

    });
