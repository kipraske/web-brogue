define([
    "jquery",
    "underscore",
    "backbone",    
    "views/console-cell"
    ], function( $, _, BackBone, ConsoleCell){

	var Console = Backbone.View.extend({

	    initialize : function (){

	    }

	});

	return new Console();

    });
