define([
    'jquery',
    'underscore',
    'backbone'
], function($, _, Backbone) {

    var CurrentGamesRow = Backbone.Model.extend({
        defaults: {
            // TODO - yeah model
        }
    });

    return CurrentGamesRow;

});

