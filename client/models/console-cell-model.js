define([
    'jquery',
    'underscore',
    'backbone',
    ], function( $, _, BackBone ){

	var ConsoleCellModel = Backbone.Model.extend({

	    defaults: {

	    }

	});

	return ConsoleCellModel;

    });
